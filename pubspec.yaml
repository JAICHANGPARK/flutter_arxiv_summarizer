name: flutter_arxiv_summarizer
description: "A new Flutter arxiv_summarizer project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  http: ^1.4.0
  dio: ^5.8.0+1
  freezed: ^3.0.6
  flutter_riverpod: ^2.6.1
  path_provider: ^2.1.5
  pdfx: ^2.9.2
  html: ^0.15.6
  riverpod_annotation: ^2.6.1
  flutter_markdown: ^0.7.7+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  riverpod_generator: ^2.6.5
  build_runner:

flutter:
  uses-material-design: true
